package com.bus.system.modules.planning.contract;

/**
 * Trạng thái Lịch chạy (TripSchedule)
 */
public enum TripScheduleStatus {
    ACTIVE, // Đang áp dụng
    INACTIVE, // Tạm ngưng
    EXPIRED // Hết hiệu lực
}
