spring:
  jpa:
    show-sql: false

seed:
  runner:
    enabled: false
    dry-run: true

security:
  rate-limit:
    enabled: false

logging:
  level:
    root: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO
    com.bus.system: INFO
